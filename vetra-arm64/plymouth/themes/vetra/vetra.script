image = Image("vetra-logo.png");
sprite = Sprite();

sprite.SetImage(image);
sprite.SetX(Window.GetX() + (Window.GetWidth() / 2 - image.GetWidth() / 2));
sprite.SetY(Window.GetY() + (Window.GetHeight() / 2 - image.GetHeight() / 2));